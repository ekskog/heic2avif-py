apiVersion: v1
kind: ConfigMap
metadata:
  name: heic2avif-py-config
  namespace: webapps
data:
  # FastAPI configuration
  PORT: "3000"
  HOST: "0.0.0.0"
  ENVIRONMENT: "production"
  
  # PhotoVault API callback configuration
  PHOTOVAULT_API_URL: "http://photovault-api-service.webapps.svc.cluster.local:3001"
  CALLBACK_TIMEOUT: "30"
